const isEven = (num) => {
    if (num % 2 === 0) {
        return true;
    }
    else {
        return false;
    }
};

// complexity O(1), complexity never changes
// always performs same number of steps- constant time
